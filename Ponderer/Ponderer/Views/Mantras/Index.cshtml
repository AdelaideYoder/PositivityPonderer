@model IEnumerable<Ponderer.Models.Mantra>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MorningMantra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlaceFound)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ApplicationUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MorningMantra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlaceFound)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicationUser.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MantraId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MantraId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MantraId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
